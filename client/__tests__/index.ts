import React from 'react';
import { shallow, mount, render } from 'enzyme';
// import App from '../components/App.jsx';

describe('TEST', ()=>{
  test('works', ()=>{
    expect(2).toEqual(2);
  })
});